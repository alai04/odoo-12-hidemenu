<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hidemenu.list">
      <field name="name">hidemenu list</field>
      <field name="model">hidemenu.hidemenu</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hidemenu.action_window">
      <field name="name">hidemenu window</field>
      <field name="res_model">hidemenu.hidemenu</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hidemenu.action_server">
      <field name="name">hidemenu server</field>
      <field name="model_id" ref="model_hidemenu_hidemenu"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hidemenu" id="hidemenu.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hidemenu.menu_1" parent="hidemenu.menu_root"/>
    <menuitem name="Menu 2" id="hidemenu.menu_2" parent="hidemenu.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hidemenu.menu_1_list" parent="hidemenu.menu_1"
              action="hidemenu.action_window"/>
    <menuitem name="Server to list" id="hidemenu" parent="hidemenu.menu_2"
              action="hidemenu.action_server"/>
    -->
  </data>
</odoo>